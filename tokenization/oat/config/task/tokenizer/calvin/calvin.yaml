name: calvin

task_name: calvin

shape_meta: &shape_meta
  action:
    shape: [7]

dataset:
  _target_: oat.dataset.zarr_dataset.ZarrDataset
  zarr_path: /home/ubuntu/VLA-in-reverse-VA-to-L-classifier/data/calvin_N500.zarr
  obs_keys: []
  action_key: action
  n_obs_steps: 0
  n_action_steps: ${horizon}
  seed: ${seed}
  val_ratio: 0.1
